var counter = 0;

function count() {
    counter++;
    document.getElementById('score').innerHTML = "score: " + counter;
}

function
switch () {
    document.getElementsById('ball').animation = "jump";
}
